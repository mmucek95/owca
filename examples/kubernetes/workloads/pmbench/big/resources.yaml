apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: stress
spec:
  template:
    spec:
      containers:
        - name: pmbench
          resources:
            requests:
              cpu: 8
              memory: 80G
            limits:
              cpu: 8
              memory: 80G

