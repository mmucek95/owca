nameSuffix: -hmem-stream-small
bases:
- ../base

commonLabels:
  app: hmem-stress-stream-small
  size: small

commonAnnotations:
  cri-resource-manager.intel.com/memory-type: |
    stress: dram,pmem
  cri-resource-manager.intel.com/cold-start: |
    stress:
      duration: 60s

images:
- name: stress_ng
  newName: 100.64.176.12:80/wca/stress_ng
  newTag: master

configMapGenerator:
- name: stress
  literals:
    - extra='--stream 1 --stream-l3-size=512m --stream-index=0' # 6GB RSS

patchesStrategicMerge:
  - resources.yaml
