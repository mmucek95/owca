nameSuffix: -small-hmem
resources:
- ../base

commonLabels:
  app: memcached-mutilate-small-hmem

configMapGenerator:
- name: mutilate
  behavior: merge
  literals:
  - load_records=800000  #600000
  - records=120000    # 20%
  - value=20000      # 80kB * 150K <= 12Gb   old80kB * 1.5M  <= 120GB
  - qps=47000 #50000 # 130000
#  - threads=3

#- name: memcached
#  behavior: merge
#  literals:
#  - memory=20000 # MB a.k.a. 20G
#  - threads=2

patchesStrategicMerge:
  - resources.yaml
